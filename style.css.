:root {
  --bg: #f7faf7;
  --card: #ffffff;
  --text: #203224;
  --muted: #58705f;
  --accent: #2d8b57;
  --accent-dark: #1f6a41;
  --border: #d8e7dc;
  --warning: #e2a33a;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: Inter, system-ui, -apple-system, sans-serif;
  color: var(--text);
  background: var(--bg);
}

.topbar {
  background: linear-gradient(120deg, #1f6a41, #2d8b57);
  color: #fff;
  padding: 1rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 1rem;
}

h1 {
  margin: 0;
  font-size: 1.4rem;
}

.subtitle {
  margin: 0.25rem 0 0;
  opacity: 0.9;
  font-size: 0.92rem;
}

.container {
  max-width: 1080px;
  margin: 1rem auto 2rem;
  padding: 0 1rem;
  display: grid;
  gap: 1rem;
}

.card {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 1rem;
  box-shadow: 0 2px 12px rgba(26, 53, 35, 0.05);
}

.grid-two {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
}

.panel,
.form-grid {
  display: grid;
  gap: 0.7rem;
}

label {
  display: grid;
  gap: 0.35rem;
  font-size: 0.9rem;
}

input,
select,
button {
  font: inherit;
  border-radius: 8px;
}

input,
select {
  padding: 0.58rem 0.65rem;
  border: 1px solid var(--border);
  background: #fff;
}

button {
  border: none;
  background: var(--accent);
  color: #fff;
  padding: 0.62rem 0.9rem;
  cursor: pointer;
  font-weight: 600;
}

button:hover {
  background: var(--accent-dark);
}

button.secondary {
  background: #eff6f1;
  color: var(--text);
  border: 1px solid var(--border);
}

.user-chip {
  background: rgba(255, 255, 255, 0.18);
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 999px;
  padding: 0.45rem 0.75rem;
  font-size: 0.85rem;
}

.dashboard-head {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 0.75rem;
}

.muted,
.meta {
  color: var(--muted);
  margin: 0.15rem 0;
  font-size: 0.9rem;
}

.list {
  display: grid;
  gap: 0.75rem;
}

.product-item,
.order-item {
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 0.6rem;
  background: #fcfefc;
}

.product-item {
  display: grid;
  grid-template-columns: 120px 1fr;
  gap: 0.7rem;
}

.product-photo {
  width: 100%;
  height: 120px;
  object-fit: cover;
  border-radius: 8px;
  background: #e5efe7;
}

.product-head {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 0.4rem;
}

.product-name,
.order-title,
h2,
h3,
h4 {
  margin: 0;
}

.actions {
  margin-top: 0.45rem;
  display: flex;
  gap: 0.4rem;
  flex-wrap: wrap;
}

.badge {
  display: inline-block;
  border-radius: 999px;
  padding: 0.2rem 0.65rem;
  font-size: 0.78rem;
  border: 1px solid transparent;
}

.price-badge {
  background: #ecf7ef;
  color: #1d6d40;
  border-color: #c7e3d2;
}

.status-prenotato {
  background: #f5f2e7;
  color: #866319;
  border-color: #ead9a9;
}

.status-spedito {
  background: #e7f0fa;
  color: #245c86;
  border-color: #bad3ec;
}

.status-consegnato {
  background: #e9f8ec;
  color: #1f6a41;
  border-color: #bde4c8;
}

.order-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 0.5rem;
}

.hidden {
  display: none !important;
}

.toast {
  position: fixed;
  left: 50%;
  transform: translateX(-50%);
  bottom: 1rem;
  padding: 0.6rem 0.8rem;
  border-radius: 8px;
  background: #21372a;
  color: #fff;
  font-size: 0.9rem;
  z-index: 30;
}

@media (max-width: 780px) {
  .grid-two {
    grid-template-columns: 1fr;
  }

  .product-item {
    grid-template-columns: 1fr;
  }

  .product-photo {
    height: 180px;
  }
}


.catalog-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
  margin-top: 40px;
}

.product-card {
  background: #ffffff;
  padding: 20px;
  border-radius: 12px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.08);
  transition: transform 0.2s ease;
}

.product-card:hover {
  transform: translateY(-5px);
}

.product-card h3 {
  margin-top: 0;
}

.price {
  font-size: 18px;
  font-weight: bold;
  color: #2e7d32;
}
